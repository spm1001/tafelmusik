# Handoff — 2026-03-30

session_id: 8a78c485-113e-41d0-873a-b9635804cbee
purpose: Re-anchor comments after replace_section — CRDT-scoped blast radius

## Done
- Completed tfm-mafuze: re-anchor comments after destructive edits (replace_section, replace_all, patch)
- Built comments.py with collect_affected + reanchor — uses StickyIndex position resolution to scope blast radius
- Orphaned comments appear greyed out at bottom of browser pane, one-click resolve
- Added Shift+Enter as compose submit shortcut (mobile support)
- Fixed false-match bug: first implementation searched whole document, now scoped to replaced section
- Patch mode re-anchoring, corrupted anchor warning logging, 2 integration tests over the wire
- Updated CLAUDE.md with re-anchoring docs, updated tfm-becitu brief
- 103 tests passing

## Gotchas
- First implementation did naive `content.find(quote)` across the whole document. Live testing caught a false match — "the weather" in Step 1 matched "The weather comment should orphan" in Step 2. The fix uses StickyIndex position resolution to only collect comments inside the blast radius, and searches only within the new section bounds.
- `StickyIndex.from_json(data, text)` — args are (data, sequence), not (text, data). `si.get_index()` not `si.index`.
- `.hypothesis/` directory is untracked in git. From property-based tests in earlier sessions. Cosmetic.

## Risks
- If a comment's quote text appears multiple times within the same section, `reanchor` picks the first match. Could re-anchor to the wrong occurrence. Unlikely with real comments (quotes tend to be unique within a section).
- Orphaned comments with collapsed anchors are still in the Y.Map — they accumulate over time if not resolved. No auto-cleanup.

## Next
- tfm-melemu is still open — tfm-becitu (extract MCP comment tools to comments.py) is the natural next action
- tfm-mabufa (non-transitive sort) and tfm-gosifa (frontend tests) are comment-adjacent work
- The files-on-disk pivot (tfm-calute) remains the deeper architectural question

## Commands
```bash
uv run pytest src/ -v                              # 103 tests
bon list                                           # Full hierarchy
git log --oneline -5                               # Recent commits
```

## Reflection
**Claude observed:** The most valuable moment was when live testing immediately caught a bug that unit tests missed — the naive whole-document search false-matched across sections. Sameer's pushback ("doesn't the CRDT cleverness help us here?") led to the correct design: use StickyIndex to determine blast radius, use quote search only within that scope. The first implementation was wrong not because of a code bug, but because I defaulted to string operations when the CRDT already had the positional information I needed.

**User noted:** Mobile testing was what caught the false match. Also surfaced a missing mobile affordance (Cmd+Enter doesn't exist on phones — added Shift+Enter). The "do it now" instinct was right — all three follow-up items (patch re-anchoring, corrupted anchor logging, integration tests) took ~15 minutes total with full context loaded.
