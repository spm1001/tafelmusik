Session 2026-04-02/03: Channel notifications fixed, cross-machine validated, bon restructured.

The "channel notifications silently dropped" bug that persisted for weeks was a one-line fix: `drift` was sent as an int in the channel notification meta, but CC's Zod schema requires `z.record(z.string(), z.string())` — all meta values must be strings. Non-string values silently fail validation. The notification was being sent correctly (confirmed via file-based debug logging), CC was dropping it at the Zod gate. This was NOT a CC bug, NOT a timing issue, NOT an idle-state problem. Cast `str(drift)` and comment notifications started arriving immediately.

Cross-machine MCP validated end-to-end. Plugin installed on Mac via marketplace, MCP server connects to hezza:3456 over tailscale. Channel flag on Mac is `plugin:tafelmusik@batterie-de-savoir` (not `server:tafelmusik`). Both initial pushes and comment notifications delivered. The Mac Claude successfully inspected docs and added comments visible in the browser.

ASGI server hung twice — the immediate cause was `_scan_doc_files()` doing synchronous rglob across ~/Repos (851+ files) on every /api/rooms request, blocking the async event loop. Fixed with 30s TTL cache. Deeper issue: 8x duplicated logging doc (CRDT duplication from repeated server restarts) consuming memory. SQLite flushed (3905 stale updates removed).

CC session ID is available at `~/.claude/sessions/{PID}.json` — contains sessionId UUID, cwd, startedAt. This enables routing tmux comments to the correct Claude session.

Bon hierarchy restructured: new outcome tfm-wijono ("Comments are a standalone messaging layer with content-addressed anchoring") with 9 ordered actions. tfm-calute slimmed to infrastructure actions. Architecture doc written (docs/architecture-v2.md).
