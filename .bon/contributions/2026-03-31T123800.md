MockSession is a blind spot in the notification test suite. Every test from Phase 2 (semame, nocaga, napari, wiveje) verifies the observer→queue→consumer→send chain but MockSession.send_message() is just list.append(). The real ServerSession.send_message() goes through the MCP stdio transport — format conversion, stream writing, client-side parsing. When we tried to verify wiveje end-to-end in production (two CC sessions, one with --dangerously-load-development-channels), comments synced to the Y.Map (list_comments confirmed 3 comments) but no notification ever reached the receiving CC session. This means the "comments as turn signal" architecture may have never worked in production — every Phase 2 feature was tested against the mock, not the real transport. The fix is tfm-webije: an e2e test that starts a real MCP server with stdio transport and verifies a client receives channel notifications.
