# Handoff — 2026-03-29

session_id: 51fd2d43-c65c-45c1-a6a4-8addc67b4ccb
purpose: Reviewed and fixed tafelmusik plan, scaffolded repo, filed 6 bon outcomes

## Done
- Reviewed plan with 4 parallel persona agents (coherence, feasibility, scope-guardian, adversarial) — found 4 P0 contradictions
- Applied 7 auto-fixes + 9 batch confirms across plan, design doc, and requirements doc
- Resolved 11 scope/design findings with user decisions: channels (not polling), CRDT comments (not inline markdown), multi-document, 6 units kept, point+quote anchoring
- Spiked pycrdt client → ASGIServer WebSocket connection — bidirectional sync validated, 3 API gotchas captured (Gotchas 16-18)
- Scaffolded repo: agent-native layout, tests adjacent to source, 3 entry points, CLAUDE.md, hooks, systemd service, plugin.json
- Filed 6 bon outcomes (tfm-tesera through tfm-hubipu) encoding the full implementation plan
- Moved plan/design/requirements from ~/Repos/docs/ into the repo, left redirect pointers
- Fixed version regex (TOML literal string escaping), Gotcha 5 (structured diffs not prose), design principle 5 (point+quote)
- Wrote .bon/understanding.md and first contribution

## Gotchas
- `WebsocketServer.start()` blocks forever — must be `asyncio.create_task()`, never awaited directly
- `HttpxWebsocket(ws, room_name)` wraps an existing httpx-ws connection, not a URL. Pattern: `async with aconnect_ws(url, client) as ws: channel = HttpxWebsocket(ws, room)`
- `get_room()` is a coroutine — must be awaited
- Working spike is at `references/prototypes/spike-connection-working.py`
- The ensure-tafelmusik.sh hook assumes systemd user unit is installed — first run will fail until `tafelmusik.service` is installed
- Starlette + ASGIServer lifespan composition not yet spiked (Gotcha 15) — will surface immediately in Unit 1

## Risks
- Unit 1 lifespan handling (starlette vs ASGIServer) is the first unspiked technical unknown
- Channel server (Unit 3) has three stacked unknowns: low-level MCP Server API, sync→async bridge, semantic diff
- StickyIndex re-anchoring after replace_section is designed but not tested — Unit 5 execution note says "exploratory"

## Next
- Start with `bon show tfm-tesera` and draw down Unit 1 (ASGI server + web editor)
- Run `uv sync` first (already done this session but .venv not committed)
- The CodeMirror prototype at `references/prototypes/cm-entry2.js` is the starting template for `public/cm-entry.js`
- After Unit 1, the starlette lifespan question will be resolved and Unit 2 (MCP) can follow immediately

## Commands
```bash
bon show tfm-tesera          # Unit 1: ASGI server + web editor
bon list                      # All 6 outcomes
uv run pytest src/            # Verify toolchain (0 tests, exit 5 = expected)
cat docs/plan.md              # Full implementation plan with 18 gotchas
```

## Reflection
**Claude observed:** Plan review was the right call — caught a fundamental process architecture contradiction that would have forced mid-implementation rewrite. The spike validated the core connection pattern but also revealed API landmines that no review could have caught. Scope-guardian recommendations were too conservative for a tool both users will live in — agent-native principles (push > pull, structured > unstructured) took precedence over "ship fast."
**User noted:** "These are amusingly conservative — they are like human reviewers not Claudes." Channels, CRDT comments, multi-doc, editor-agnostic — all kept. Point+quote comment anchoring came from user's casual question and is a meaningfully better architecture. "Don't leave for tomorrow's Claude what today's has better context for."
