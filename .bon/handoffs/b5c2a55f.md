# Handoff — 2026-03-31

session_id: b5c2a55f-fe84-4b6b-be69-4f255f482c49
purpose: URL routing, file browser, editor polish (dibuca + lefato + ridawi)

## Done
- Diagnosed notification pipeline as CC-side bug, closed tfm-zacohu and tfm-webije
- URL path routing — room name IS the URL path, WebSocket at /_ws/ prefix (tfm-dibuca)
- Default docs_dir changed to ~/Repos, file listing filters dotdirs/node_modules
- File browser sidebar with search, active/recent sections, resize handle
- rAF debounce on Y.Map observer to prevent DOM thrashing (tfm-lefato partial)
- Compose card no longer steals focus on selection
- Markdown formatting shortcuts: Cmd+B/I/E/Shift+X/Shift+. (tfm-ridawi)
- Reverted debug file handler logging from mcp_server.py
- Updated CLAUDE.md with routing changes

## Gotchas
- WebSocket route is now /_ws/{room:path} — old ?room= URLs and direct /{room} WebSocket connections are broken
- console.warn on connection status changes still in cm-entry.js for lefato diagnosis
- lefato left open — blip couldn't be reproduced without MCP peer connected
- /api/rooms scans ~/Repos on every request (851 files) — filed tfm-relefu for caching

## Risks
- Old bookmarks/sessions using ?room= will silently open an empty "default" room
- The rglob scan in /api/rooms could become slow as ~/Repos grows — no caching yet

## Next
- Test lefato blip with MCP peer connected — console logging is in place
- Editor polish remaining: wiseha (stale Y.Doc), vevure (line numbers), mabufa (sort), gosifa (frontend tests)
- calute remaining: pelula (eviction), rohudu (spawn-on-comment), rinoga (drift tuning), relefu (scan caching)

## Commands
```bash
# Run all tests
uv run pytest src/tafelmusik/ -v

# Rebuild JS after editing cm-entry.js
cd public && npx esbuild cm-entry.js --bundle --outfile=editor.js --minify

# Check service
journalctl --user -u tafelmusik --since "10 min ago" --no-pager
```

## Reflection
**Claude observed:** The compose-card focus problem was a design issue, not a code issue — selection and commenting share the same gesture but are different intents. Four iterations to find that "no auto-focus" is the right default. Also, ~/Repos as docs_dir was inevitable but the 1376→851 file listing shows filtering isn't enough — caching is needed.
**User noted:** The file browser "was a nightmare" with all files shown. Livable after active+recent default view with search. The selection/focus asymmetry is annoying but accepted as a browser primitive.
