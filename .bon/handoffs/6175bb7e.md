# Handoff — 2026-03-30

session_id: 6175bb7e-1701-40b6-9881-f58fd0bdd9c7
purpose: Surgical editing (patch mode), room awareness (poller), UI design captured from live doc

## Done
- Completed tfm-zeguhi: surgical editing — `document.patch()` with content-addressed find/replace, h1 guard on `replace_section`, all 5 editing-grammar scenarios tested
- Completed tfm-ditufu: room poller discovers rooms without tool calls (5s interval, error-tolerant)
- Completed tfm-hinudi (parent outcome): push notifications fully wired — observer, channel notifications, room awareness
- Review pass: `heading_level` made public, `append` mode uses `authors.CLAUDE`, content validation on edit_doc, pytest.raises consistency
- Filed tfm-calute: documents as files on disk (architectural pivot — not started)
- Filed tfm-kosotu, tfm-keguka, tfm-dibuca: UI outcomes from Sameer's scribbles in default room
- Reshaped default room doc with grouped UI notes and bon IDs
- Updated CLAUDE.md: patch mode, heading_level, room poller, squashing disabled
- Updated docs/editing-grammar.md: v0.2 tool table, Solution subsections for all 5 scenarios

## Gotchas
- MCP tool schema for `edit_doc` won't show `find`/`replace` params until MCP server restarts. Live sessions see old 3-param schema.
- Room poller connects to ALL rooms including test/scratch. No filtering. Fine at 8 rooms, chatty at scale.
- `_poll_rooms` swallows all exceptions with `log.debug` — failures invisible at default log level.
- `content` param on edit_doc went from required to optional (default empty string). Validation catches empty content for modes that need it, but `replace_all` with `content=""` intentionally allowed (legitimate "clear document" operation).
- `replace_all` in Edit tool is dangerous for renaming — it mangled `test_heading_level_detection` into `testheading_level_detection` during the `_heading_level` → `heading_level` rename. Caught and fixed, but watch for this.

## Risks
- Room poller has no concurrency limit — 50 simultaneous new rooms = 50 concurrent `connect()` calls.
- Property-based tests now start at level 2 (not 1) due to h1 guard. This means h1-related edge cases in replace_section are no longer fuzz-tested — only the explicit scenario 4 test covers h1.
- pycrdt-store upstream PR (y-crdt/pycrdt-store#25) may still be unmerged. Squashing remains disabled.

## Next
- tfm-kosotu is the gateway to comments: strip the UI, then repurpose split pane as comments sidebar
- tfm-melemu (comments) — backend exists (StickyIndex + Y.Map in comments.py), gap is frontend affordance
- Sameer's sharpest framing of comments: "I want to react to bits of this rather than paste-and-interleave" — use this to anchor the design
- Dependency chain: tfm-kosotu → comments pane UI → tfm-melemu backend wiring
- tfm-calute (files on disk) is a deeper architectural question — would change room discovery, naming, lifecycle. Not urgent but foundational.
- Pretext (chenglou/pretext) — text measurement library, not relevant for current UI work but interesting for custom rendering

## Commands
```bash
uv run pytest src/ -v                              # 85 tests
bon list                                           # Full hierarchy (7 open)
gh pr view 25 -R y-crdt/pycrdt-store               # Upstream squash fix
```

## Reflection
**Claude observed:** The review pass was the most valuable part — shipping then reviewing caught real issues (h1 guard in wrong layer, content default footgun, heading_level crossing module boundary as private). The architectural tangent about files-on-disk was worth the detour: it reframed tfm-ditufu's room poll as a pragmatic stopgap rather than the right answer, and connected several of Sameer's UI scribbles into a coherent direction.

**User noted:** The strongest signal was frustration during /close itself — wanting to react inline to specific parts of the Orient reflection. "I want to react to bits of this" is the clearest articulation of why comments matter. Also flagged that instincts about architecture (where documents live) aren't always easy to connect to technology choices, but the connections matter. Pretext was a lateral idea — not the right tool here, but the instinct to avoid CSS pain is valid.
