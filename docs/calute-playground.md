SO I'm here but it's blank? << that was there before so somehow the hydrate did an append?

# Calute Playground

This file lives on disk at `docs/calute-playground.md`.

## What to try

- Edit this text in the browser — it's live in the CRDT
- The file on disk won't change until `flush_doc` is called
- Check `list_docs` — this file should appear

## Some content to play with

The quick brown fox jumps over the lazy dog.
# Calute Playground

This file lives on disk at `docs/calute-playground.md`.

## What to try

- Edit this text in the browser — it's live in the CRDT
- The file on disk won't change until `flush_doc` is called
- Check `list_docs` — this file should appear

## Some content to play with

The quick brown fox jumps over the lazy dog.
