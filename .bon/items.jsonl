{"id": "tfm-fujosu", "type": "outcome", "title": "Claude reads and edits documents as a Yjs peer via MCP", "brief": {"why": "Without MCP tools, Claude can't participate in the collaboration. The MCP server is Claude's hands — read_doc, edit_doc, replace_section, load_doc. This is what makes Claude a peer rather than a bystander.", "what": "1. Create mcp_server.py — FastMCP with stdio transport, connects to ASGI server via WebSocket as pycrdt client (HttpxWebsocket + Provider pattern validated in spike) 2. Create document.py — Y.Text operations: read (str(text)), edit (replace/append/replace_section), load (clear + write from markdown). replace_section finds heading by text match, replaces until next heading of equal/higher level 3. Implement tools: read_doc(room), edit_doc(room, content, mode), load_doc(room, markdown), list_docs() 4. Create .claude-plugin/plugin.json with mcpServers config (TAFELMUSIK_URL env var) 5. Create hooks/ensure-tafelmusik.sh — SessionStart hook checks uv, .venv, ASGI server running, handles stale port cleanup 6. Test: read_doc returns markdown matching editor; edit_doc changes appear in browser; integration round-trip both directions", "done": "mcp__tafelmusik__read_doc() works in a Claude Code session. Edits via MCP appear in browser without refresh. Browser edits readable via MCP."}, "status": "open", "order": 2, "created_at": "2026-03-29T01:10:51Z", "created_by": "spm1001"}
{"id": "tfm-hinudi", "type": "outcome", "title": "Claude receives push notifications when Sameer edits", "brief": {"why": "Without change notification, Claude only discovers edits when explicitly asked to read. Channels break the turn-taking model — Claude knows Sameer reordered section 3 without being told. This is what makes the collaboration feel live rather than relay-based.", "what": "1. Create channel_server.py — low-level MCP Server API (not FastMCP), captures ServerSession for push notifications. Connects to ASGI server as pycrdt Yjs client via WebSocket 2. Observe Y.Text via text.observe() — synchronous callback uses asyncio.Queue.put_nowait(), async consumer task debounces (2s window) and sends notifications 3. Semantic diff: cache previous markdown, parse headings to identify changed section, produce structured change summary (section name + changed lines) — not NL prose, Claude reads diffs better 4. Send via ServerSession.send_message() with JSONRPCNotification(method='notifications/claude/channel') 5. Distinguish edit sources to avoid self-notification 6. Update plugin.json with channel server config", "done": "Claude Code session with channels enabled receives notification when browser edits the doc. Claude edits via MCP produce no self-notification. Rapid edits debounced into single notification."}, "status": "open", "order": 3, "created_at": "2026-03-29T01:11:05Z", "created_by": "spm1001"}
{"id": "tfm-hubipu", "type": "outcome", "title": "Documents export cleanly to Google Docs with images intact", "brief": {"why": "Google Docs is the delivery format — when documents need to reach other humans (stakeholders, team members), they go to Docs. Without export, every finished document requires manual copy-paste. Images are the hard part: Google's API fetches images server-side and can't reach Tailscale URLs.", "what": "1. Add export_to_docs(title) tool to mcp_server.py — reads Y.Text (already markdown), writes to a mise deposit folder, calls mise do(operation='create', source=...) 2. Handle images: for each image URL in the markdown, use mise's upload-share-insert-revoke pattern (temporary public URL for Google's servers to fetch). This is a Google API limitation, not a choice 3. Test: export document with headings, tables, lists, code blocks — all formatting survives. Export document with images — images appear in Google Doc", "done": "export_to_docs('Q1 Report') creates a Google Doc with correct formatting. Headings, tables, lists, bold/italic, code blocks intact. Images present via mise upload-share-insert-revoke pattern."}, "status": "open", "order": 6, "created_at": "2026-03-29T01:11:44Z", "created_by": "spm1001"}
{"id": "tfm-melemu", "type": "outcome", "title": "Comments anchored to text flow bidirectionally between Sameer and Claude", "brief": {"why": "Comments are the collaboration mechanism — 'expand this section', 'make this punchier', 'I moved this'. Without anchored comments, feedback happens in chat, disconnected from the text. This is what makes Tafelmusik more than a shared text buffer.", "what": "1. Create comments.py — Y.Map 'comments' in Y.Doc, keyed by comment ID. Each comment: {anchor: StickyIndex, quote: str, author, body, resolved}. Point+quote architecture: single anchor provides location, stored quote provides resilience and re-anchoring after replace_section. Use StickyIndex.new(text, idx, Assoc.AFTER) and encode()/decode() for storage 2. Add MCP tools to mcp_server.py: add_comment(quote, body) — find quote text, create StickyIndex at match position, store in Y.Map. list_comments() — return all comments with current anchor positions and quote text. resolve_comment(id) — mark resolved 3. Add re-anchoring to document.py replace_section: before replacing, collect comments in affected range; after replacing, search for quote text and re-anchor; orphan comments whose quote was deleted 4. Add CodeMirror comment decorations to cm-entry.js: read Y.Map, resolve StickyIndex to offset, search forward for quote text, underline match. Select text + Cmd+M opens comment input 5. Rebuild JS bundle", "done": "Claude adds comment via add_comment, Sameer sees underlined text with popover. Sameer adds comment via Cmd+M, list_comments returns it. Comments survive concurrent edits. Replace_section re-anchors comments whose quote text survived."}, "status": "open", "order": 5, "created_at": "2026-03-29T01:11:33Z", "created_by": "spm1001"}
{"id": "tfm-neceki", "type": "outcome", "title": "Images drag-and-drop into the editor and persist on hezza", "brief": {"why": "Documents without images are incomplete. Sameer needs to paste screenshots, diagrams, photos into the editor without leaving the browser. Images must persist across restarts and be accessible over Tailscale for Google Docs export. Images must NOT enter Claude's conversation context — file deposit pattern.", "what": "1. Add POST /upload route to asgi_server.py — multipart, saves to data/uploads/<uuid>.<ext>, MIME type validation for common image formats 2. Create uploads.py — upload handling logic, UUID filename generation, MIME type mapping 3. Add GET /uploads/* static file serving to asgi_server.py 4. Add drop/paste handler to cm-entry.js — intercept file drops and clipboard pastes, upload via fetch, insert \\![filename](url) at cursor position 5. Rebuild JS bundle after editor changes 6. Test: drag image into editor, markdown image reference inserted, image accessible via URL from Mac, image persists across server restart, non-image file handled gracefully", "done": "Image visible in preview pane after drag-and-drop upload. URL accessible from Mac via Tailscale. Images persist across server restarts."}, "status": "open", "order": 4, "created_at": "2026-03-29T01:11:17Z", "created_by": "spm1001"}
{"id": "tfm-tesera", "type": "outcome", "title": "Browser-based collaborative editor serves Y.Text documents", "brief": {"why": "Foundation for everything else — without the ASGI server holding a Y.Doc and serving CodeMirror, no other unit can function. Sameer needs to edit markdown in a browser; Claude needs a WebSocket endpoint to connect to.", "what": "1. Create asgi_server.py — pycrdt-websocket ASGIServer + starlette routes, SQLiteYStore persistence, port 3456 2. Create public/index.html + cm-entry.js — CodeMirror 6 with y-codemirror.next binding, split/preview/edit tabs, markdown preview with marked, table/code/blockquote CSS 3. Bundle JS with esbuild (npx esbuild public/cm-entry.js --bundle --outfile=public/editor.js --minify) 4. Create pyproject.toml with all deps, CLAUDE.md, .gitignore 5. Verify: two browser tabs edit simultaneously and sync; content persists across server restart; server restarts while browser connected — reconnects cleanly", "done": "Editor loads from Mac via Tailscale IP. Two tabs sync. Content survives server restart. Server restart with connected browser reconnects cleanly."}, "status": "open", "order": 1, "created_at": "2026-03-29T01:10:37Z", "created_by": "spm1001"}
{"id": "tfm-vocego", "type": "action", "title": "Wire ASGI server + CodeMirror editor with live sync", "brief": {"why": "Can't test or build anything until the server holds a Y.Doc and serves an editor that syncs via WebSocket", "what": "1. Create asgi_server.py — ASGIServer + Starlette routes + SQLiteYStore 2. Create cm-entry.js from cm-entry2.js prototype + index.html 3. Bundle with esbuild 4. Verify: two tabs sync, persistence survives restart, reconnect after server restart", "done": "Editor loads at :3456 from browser, two tabs sync edits, content persists across server restart, browser reconnects after server restart"}, "status": "done", "parent": "tfm-tesera", "order": 1, "created_at": "2026-03-29T01:28:58Z", "created_by": "spm1001", "waiting_for": null, "tactical": {"steps": ["Create asgi_server.py — ASGIServer + Starlette routes + SQLiteYStore", "Create cm-entry.js from cm-entry2.js prototype + index.html", "Bundle with esbuild", "Verify: two tabs sync, persistence survives restart, reconnect after server restart"], "current": 4, "session": "/home/modha/Repos/batterie/tafelmusik"}, "updated_at": "2026-03-29T01:41:45Z", "updated_by": "stepped", "done_at": "2026-03-29T01:41:45Z"}
