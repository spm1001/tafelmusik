# Handoff — 2026-03-30

session_id: 6f7349ae-7212-4e05-88f0-bc5a6bb90ac7
purpose: Calute design session + spike — files on disk as CRDT overlay

## Done
- Closed tfm-melemu (all three actions complete, done criteria met)
- Designed calute (tfm-calute) end-to-end: three phases, all key decisions resolved
- Updated tfm-calute bon brief with concrete 11-step --what and phased --done criteria
- Wrote implementation brief (.bon/contributions/2026-03-30T163000.md) with specific files, pseudocode, gotchas
- Loaded calute-design doc into Tafelmusik room with inline comments on each design decision
- Built calute spike test (calute_spike_test.py): 9 tests proving hydrate → edit → comment → flush → re-hydrate round-trip
- Validated state vector drift score as native CRDT metric (doc.get_update(snapshot) byte size)
- Research: read full Peritext essay and Local-first software essay, extracted relevant patterns
- 112 tests passing (103 existing + 9 spike)

## Gotchas
- When creating nested Y.Maps (comments), integrate into parent FIRST then set properties. comments_map[id] = inner_map before inner_map["key"] = value. Reversed order raises RuntimeError.
- text += content appends, doesn't replace. For hydrate, the Doc/Text must be fresh.
- The calute-design doc in Tafelmusik has orphaned comments from a full rewrite — cosmetic, not a bug.
- Tafelmusik isn't reachable from Shellfish/phone (probably Tailscale not active on phone).

## Risks
- No integration test yet for hydrate-from-file through the ASGI server's RoomManager (spike tests are unit-level with pure pycrdt). Phase 1 implementation needs this.
- Migration path: existing rooms have SQLite state but no .md files. The hydrate logic tries file first, falls back to SQLite. Testing this transition matters.
- Flush is explicit (Claude-initiated) — if a session crashes before flush, edits exist only in the CRDT/SQLite. This is acceptable (CRDT handles crash recovery during session) but worth noting.

## Next
- Phase 1 of calute: implement in asgi_server.py and mcp_server.py using the implementation brief
- The spike tests (calute_spike_test.py) validate the core lifecycle — build on them for integration tests
- The calute-design room in Tafelmusik has the full design rationale
- Comment cleanup items (becitu, mabufa, gosifa) slot in at natural pauses

## Commands
\`\`\`bash
uv run pytest src/ -q                                    # 112 tests
uv run pytest src/tafelmusik/calute_spike_test.py -v     # Spike tests only
bon show tfm-calute                                      # Full calute plan
\`\`\`

## Reflection
**Claude observed:** The most productive pattern was following the user's tangents. What started as "review the bons" became a deep design session that resolved all five open questions for calute, discovered that CRDTs are already more token-efficient than files (contrary to initial assumption), identified the comment-as-turn-signal protocol, and found native CRDT machinery (state vector drift) we were about to reinvent. The spike at the end caught a real pycrdt gotcha (Map integration order) that would have cost debugging time later. Research into Peritext and Local-first validated design decisions rather than changing them — but the Awareness protocol pattern is genuinely new information worth studying for Phase 2.

**User noted:** Token efficiency matters — hauling content in and out of context is wasteful. The drift threshold is a Goldilocks problem (too low = wasted resync tokens, too high = wasted failed-edit tokens). Always spawn on hezza simplifies cross-machine dispatch. The comment choreography is the key insight: comments as turn signal, not continuous document-body notifications.
