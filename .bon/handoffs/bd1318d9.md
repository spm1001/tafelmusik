# Handoff — 2026-04-03

session_id: bd1318d9-e53f-4a43-8f33-2e2d428dd945
purpose: Channel notifications fixed, bon restructured for comment migration

## Done
- Fixed channel notification delivery — `drift: int` → `str(drift)` (Zod requires string meta values) (9974504)
- Cached /api/rooms rglob with 30s TTL — prevents ASGI server event loop saturation (006ccb9) (tfm-relefu)
- Cross-machine MCP validated — Mac plugin connects to hezza:3456 over tailscale, channels deliver
- Plugin v0.2.1 shipped — `ws://hezza:3456` URL, marketplace install works on Mac
- Flushed SQLite yupdates (3905 stale entries) — clears CRDT duplication
- Bon restructured — new outcome tfm-wijono with 9 ordered actions for comment migration
- Folded tfm-wiseha into tfm-salima (replace y-websocket fixes reconnection)
- Architecture doc written (docs/architecture-v2.md) — tfm-batipi done
- Marked tfm-relefu done (rglob cache shipped)

## Gotchas
- Channel notification meta values MUST be strings — CC's Zod schema silently drops non-string values with no error. Always `str()` everything in meta.
- Channel flag differs: `server:tafelmusik` on hezza, `plugin:tafelmusik@batterie-de-savoir` on Mac. Needs harmonising (tfm-tokamo).
- ASGI server still hangs under sustained load — rglob cache helps but something else accumulates (tfm-hafore). Memory was 178MB before first hang.
- CRDT duplication recurs on every server restart — flushing SQLite is a manual fix. Proper fix is tfm-salima (browser discards stale Y.Doc).
- `comments.py` (Y.Map) and `anchored.py` (SQLite) coexist — comments.py is being replaced. Don't use both.

## Risks
- The SQLite flush wiped ALL persisted CRDT state. Non-file-backed rooms (ad-hoc, never flushed) lost content. File-backed rooms re-hydrate from .md.
- The rglob 30s cache means new .md files take up to 30s to appear. Acceptable but surprising.
- The Mac plugin install wasn't version-verified — assume v0.2.1 but didn't confirm.

## Next
- **tfm-hafore** — investigate ASGI server memory/CPU accumulation (first line of enquiry)
- **tfm-jafebo** — wire anchored.py into ASGI server (HTTP endpoints + WS broadcast) — the critical path action
- **tfm-sigevi** — tmux hook POSTs to ASGI (validates HTTP design, immediately useful)

## Reflection
**Claude observed:** The channel notification bug was ours, not CC's — months of "CC drops notifications" was a one-line type mismatch. File-based debug logging found it in one attempt after extended theorising about CC internals failed. Lesson: instrument before theorising. The tmux commenting interaction model (Sameer comments while Claude works, no turn-taking) is the core value proposition worth preserving through the migration.
**User noted:** The comment system is exciting — starting with the content atom (anchored.py) and building up helped make the abstract architecture concrete. The file browser is architecturally wrong and should be replaced, not cached. Dev/prod environments should be harmonised. The ASGI server stability should be first priority next session.
