# Handoff — 2026-03-30 (auto)

session_id: 5f3663fd-b53b-4dc1-9b3c-ee41ba35b04d
purpose: Implement drift-aware context push pipeline and comment observer in MCP server; fix calute regression; restructure docs.

## Done
- **tfm-becitu** — Extracted comment operations out of `mcp_server.py` into `comments.py`
- **tfm-semame** — Replaced doc-change observer with comment observer in MCP server
- **tfm-nocaga** — Added drift score tracking and context-aware content push logic; idle timer now cancelled on high-drift comment push; snapshot cleanup added
- **tfm-rinoga** — Added drift logging to support future threshold tuning; registered as open bon for production follow-up
- **tfm-calute (partial)** — Reverted `_handle_message` intercept that broke MCP tool registration (`fe39097`); refactor of comment ops and observer are the calute sub-items that landed
- **Docs** — Restructured bons list, added Phase 2 implementation brief, updated `CLAUDE.md` with calute conventions, added contribution note on action choreography

## Next
- **tfm-wiveje** — Capture MCP session at initialization, not at first tool call
- **tfm-pelula** — Add eviction policy for file-backed rooms on long-running servers
- **tfm-napari** — Remove `read_doc` from default tool surface (system pushes, Claude never pulls)
- **tfm-rohudu** — Spawn Claude on hezza when a comment arrives with no MCP peer connected
- **tfm-rinoga** — Tune `DRIFT_THRESHOLD` using the drift logs now being emitted
- **tfm-dibuca** — File browser UI (docs_dir as ~/Repos, plain file path URLs)
- **tfm-wiseha** — Browser discards stale Y.Doc on server restart to prevent CRDT duplication
- **tfm-ridawi** — Keyboard shortcuts: Cmd+B bold, Cmd+I italic

## Gotchas
- Auto-generated handoff — no reflective close was performed
- The `_handle_message` intercept approach for tfm-calute is a dead end — it broke MCP tool registration and was reverted; any future attempt to hook early into the MCP lifecycle needs a different seam
- Drift threshold (`DRIFT_THRESHOLD`) is currently untuned; logging is in place but no production data has been collected yet — treat the current value as a placeholder
