# Handoff — 2026-03-30

session_id: 1a6d7094-08c5-4262-8c91-6e7662b77192
purpose: Restructured bons from design conversations, wrote Phase 2 implementation brief

## Done
- Verified channel notifications work end-to-end (bilateral editing in scratch/hello-channels)
- Audited all bons against design conversations from session transcripts (6f7349ae)
- Major bon restructure: 4 new Phase 2 actions (tfm-semame, tfm-nocaga, tfm-napari, tfm-rohudu)
- Converted tfm-mabufa, tfm-gosifa, tfm-becitu from standalone outcomes to actions under parents
- Moved tfm-wiseha under tfm-keguka, tfm-dibuca under tfm-calute
- Created 3 new keguka actions: tfm-ridawi (Cmd+B), tfm-vevure (line numbers), tfm-lefato (connection blips)
- Closed tfm-licota as won't-fix (symlink escape — theoretical, docs_dir moving to ~/Repos anyway)
- Updated tfm-pelula (eviction = flush + teardown), tfm-dibuca (docs_dir = ~/Repos, drop ?room=)
- Narrowed tfm-wiveje to new-doc-from-browser case only
- Updated tfm-hubipu to flush-then-export pattern
- Wrote Phase 2 implementation brief (docs/calute-phase2-brief.md) with sequencing constraints
- Filed bon-tufihu in bon repo: --how flag for implementation choreography
- Synthesized contributions into understanding.md (CRDT duplication trap)

## Gotchas
- docs_dir to ~/Repos (tfm-dibuca) has blast radius: room listing becomes all .md files in all repos, systemd service + MCP plugin config both need TAFELMUSIK_DOCS_DIR updated, path validation logic changes fundamentally (no sandbox)
- Phase 2 has a dangerous partial state: observer swapped + no drift tracking = Claude deaf to major surgery. Implementation order is mandatory: becitu then semame then nocaga then napari. See docs/calute-phase2-brief.md.
- calute-walkthrough room has 4 unresolved Sameer comments — design artefact, not production, but flush before it goes stale

## Risks
- The Phase 2 brief is a workaround for a bon limitation (no --how). If future Claudes don't read the brief, they will implement actions in isolation and hit the dangerous partial state.

## Next
- Phase 2 work: start with tfm-becitu (extract comment ops), then tfm-semame (comment observer)
- Or editor polish: tfm-ridawi (Cmd+B) and tfm-vevure (line numbers) are quick wins
- Start with --dangerously-load-development-channels server:tafelmusik and call list_docs early

## Commands
```bash
uv run pytest src/ -q                           # 127 tests
bon show tfm-calute                             # Full calute plan with Phase 2 actions
cat docs/calute-phase2-brief.md                 # Implementation choreography
```

## Reflection
**Claude observed:** The most valuable discovery was archaeological — grep didn't find the design conversation because JSONL message schema uses type: "user" + message.content, not the expected role + content. Once found, the conversation (session 6f7349ae, lines 45-155) contained a complete notification model that had been compressed to a single line in the bon. The gap between conversation richness and bon fidelity is a recurring pattern — this session's main contribution was making that gap explicit and partially filling it with the implementation brief.

**User noted:** "Why does this keep happening — we have strong conversations then they don't get captured in enough detail for future Claudes to execute." Identified the missing abstraction: bons need a --how mechanism for choreography between actions. Filed as bon-tufihu. Also: docs_dir should be ~/Repos not a sandboxed subdirectory, eviction should flush first, and the symlink security concern doesn't matter in practice.
