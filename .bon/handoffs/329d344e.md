# Handoff — 2026-04-02

session_id: 329d344e-757a-453c-ab33-38c4f05f6bd7
purpose: Architectural rethink — comments as standalone anchored messages

## Done
- Diagnosed architectural brittleness across the CRDT stack
- Ran Deep Research on collaborative editing architectures (results in ~/Taildrive/collab-analysis.md)
- Designed two-layer architecture: Yjs for text sync, SQLite for comments
- Built anchored.py — standalone comment module with TextQuoteSelector anchoring, 28 tests
- Built playground.py — interactive CLI for testing comments against real files
- Built tmux integration — Ctrl-b C popup for quick anchored reactions
- Built read-comments.py — Claude reads pending comments from SQLite
- Closed 4 superseded bons (tfm-roturo, tfm-gesegu, tfm-mabufa, tfm-rimuno)
- Filed 3 new bons: anchored comments migration (tfm-jafebo), design document (tfm-batipi), author generalisation (tfm-sinodu)

## Gotchas
- The `_find_by_context` (strategy 4) does exact prefix/suffix matching — should use fuzzy for robustness
- `comment.py` hardcodes author="sameer" — needs generalisation before multi-agent use
- `~/.tafelmusik-comments.db` is global — concurrent readers both resolve comments
- The tmux popup "multiline" is actually one long line wrapping in an 80×4 popup — it works but it's accidental. The width/height change (from 60×3 to 80×4) is what made it feel better
- Deep Research results at ~/Taildrive/collab-analysis.md — thorough findings on CM6 collab, annotation systems, Zed's CRDT, iA Writer authorship format

## Risks
- anchored.py fuzzy matcher is O(n*m) per comment — fine for dozens, untested at hundreds
- No design document yet captures the architectural decisions — risk of re-derivation
- Y.Map comment code still active in production; anchored.py is parallel, not replacing yet

## Next
- Write design document (tfm-batipi) — capture two-layer architecture decisions while they're fresh
- Wire anchored comments into ASGI server (tfm-jafebo) — HTTP endpoints + WebSocket broadcast
- The layered roadmap: 1-2 done (atom + threading), 3 next (surface — browser editor), 4 (propagation), 5 (auto re-anchor on edit), 6 (co-editing stays Yjs), 7 (authorship), 8 (export)

## Reflection
**Claude observed:** The session drifted productively from "this feels brittle" through deep research, architectural reframe (comments are messages, not annotations), schema design, prototype, and live integration — all in one session. The anchored comment tmux prototype proved the thesis: spatially-anchored reactions are easier to parse than linear text. Sameer's "limpets" metaphor captured the re-anchoring behaviour perfectly. The most important finding: both parties can read natural language, so structured metadata (motivation, payloads) adds complexity without value.
**User noted:** The serialisation problem is real — manually copy-paste-structure-paste is the current workflow and it's slow. Comments need to be frictionless (every keystroke counts). The tmux popup is workable. Rich conversations compress to terse handoffs and future Claudes can't execute from them — the design document is critical. Multiple Claudes will need distinct authorship. The bon board needed refactoring to reflect the new direction.
