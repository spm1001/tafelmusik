# Handoff — 2026-03-30 (auto)

session_id: 62ffa239-01d6-442c-81d1-bb89b44e1a48
purpose: Land drift tracking + comment observer refactor, tidy docs, close with clean tree

## Done
- **tfm-becitu**: Extracted comment operations out of `mcp_server.py` into `comments.py`
- **tfm-semame**: Replaced doc-change observer with comment observer in MCP server
- **tfm-nocaga**: Implemented drift score tracking and context-aware content push in MCP server
- **fix**: Cancelled idle timer on high-drift comment push; cleaned up stale snapshots
- **chore**: Added drift logging (threshold tuning instrumentation); removed dead `_cached_content` attribute
- **fix (tfm-calute)**: Reverted `_handle_message` intercept — it broke MCP tool registration entirely (fe39097)
- **docs**: Restructured bon list + wrote Phase 2 implementation brief
- **docs**: Added contribution note — bons need choreography between actions
- **docs**: Added calute conventions to `CLAUDE.md`; recorded tfm-rinoga as open bon for threshold tuning
- Session ended with a clean working tree, nothing uncommitted, up to date with `origin/main`

## Next
- **tfm-rinoga**: Tune `DRIFT_THRESHOLD` using production drift logs now being emitted
- **tfm-wiveje**: Capture MCP session at server initialization, not at first tool call
- **tfm-napari**: Remove `read_doc` from the default tool surface (system pushes, Claude never pulls)
- **tfm-rohudu**: Spawn Claude on hezza when a comment arrives with no MCP peer connected
- **tfm-pelula**: Add eviction policy for file-backed rooms on long-running servers
- **tfm-dibuca**: File browser UI — `docs_dir` is `~/Repos`, URLs are plain file paths
- **tfm-wiseha**: Browser should discard stale `Y.Doc` on server restart to prevent CRDT duplication
- **tfm-ridawi**: Keyboard shortcuts for markdown formatting (Cmd+B bold, Cmd+I italic)
- **tfm-neceki / tfm-hubipu**: Images (drag-and-drop + Google Docs export) — no progress yet

## Gotchas
- Auto-generated handoff — no reflective close was performed
- The `_handle_message` intercept approach (fe39097) is a confirmed dead end — do not revisit without a different hook point
- Drift logs exist now but no baseline data yet; tfm-rinoga threshold tuning needs a real workload run first
- `tfm-calute` still has two open sub-bons (tfm-wiveje, tfm-pelula, tfm-dibuca) blocking a clean Phase 2 close
